$(function () {
  var options = {
    strings: ["Coding", "Gaming", "Sitting?", "Sleeping", "Reading", "Writing", "Doing what I love"],
    typeSpeed: 120,
    loop: true,
    startDelay: 500,
    backDelay: 500,
    backSpeed: 200
  }


  var typed = new Typed(".type", options);

})
